{{- if .Values.sink.mysql.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "common.names.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  mysqlHost: {{ (include "nacos.mysql.primaryHost" .) | b64enc | quote }}
  mysqlPort: {{ "3306" | b64enc }}
  mysqlDatabase: {{ .Values.mysql.auth.database | b64enc | quote }}
  mysqlUsername: {{ .Values.mysql.auth.username | b64enc | quote }}
  mysqlUserPassword: {{ .Values.mysql.auth.password | b64enc | quote }}
{{- end }}
